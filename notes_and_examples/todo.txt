🚀 Immediate Fix Plan
✅ 1. Lock Optimization (HIGH impact)

Refactor all node.lock().await calls so they're called once at the top and reused across the function. Inside loops, avoid locking again.
✅ 2. Batch Shard Aggregation (MEDIUM impact)

Instead of inserting one shard at a time, collect a batch of (round_id, tx_index, shard_index, data) and insert all at once with a single lock.
✅ 3. Skip Early Validation (HIGH impact)

Only validate Merkle proofs after quorum is met, or add a fast-check toggle.
✅ 4. Parallelize HTTP Commits (MEDIUM impact)

Use tokio::join! or futures::future::join_all to broadcast commits concurrently to all nodes.
✅ 5. Reuse Decoder (MINOR impact)

Reuse and pre-allocate buffers for decoding base64 shards.