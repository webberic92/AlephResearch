✅ Performance Restoration Suggestions

    Pull out all config values (tx_size, data_shards, quorum, etc.) once at the top.

    Avoid node.lock().await inside loops. Cache guards or use pattern like:

    let guard = node.lock().await;
    let tx_size = guard.transaction_size;
    let nodes = guard.nodes.clone();
    drop(guard);

    Parallelize Merkle proof checks after quorum using rayon::par_iter() or tokio::task::spawn_blocking.

    Avoid re-decoding shards if you’ve already validated them in a past call (introduce a hash map cache).

    Minimize cloning of Client, commit_request, or prevote_request by wrapping them in Arc.

    Benchmark restored version against the older pre-refactor build to measure total execution time per round.

Would you like me to rewrite the handle_propose.rs and handle_prevote.rs with these specific optimizations applied?