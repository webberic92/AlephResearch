For 3 nodes:

    f=⌊(N−1)/3⌋=⌊(3−1)/3⌋=0f=⌊(N−1)/3⌋=⌊(3−1)/3⌋=0
    The required quorum is:
    2f+1=2(0)+1=1
    2f+1=2(0)+1=1

This means:

    Node 1, Node 2, and Node 3 only need 1 valid proposal to reach the quorum.
    After Node 1 proposes, both Node 2 and Node 3 have the quorum (1 proposal) and are ready to move to the prevote phase.

Why this is sufficient:

In a 3-node network:

    There are no faulty nodes (f=0f=0).
    Receiving 1 proposal guarantees data availability and integrity, satisfying the requirements of the ch-RBC proof. Thus, nodes are ready for the prevote phase after receiving 1 valid proposal.


    Yes, in your example of 3 nodes:

    Node 1 sends its proposal to Node 2 and Node 3.

    Once Node 2 and Node 3 receive the proposal from Node 1, each node has 1 valid proposal, which satisfies the quorum requirement (2f + 1) for 3 nodes because:
        f=⌊(N−1)/3⌋=0f=⌊(N−1)/3⌋=0
        2f+1=12f+1=1

    After receiving the proposal:
        Node 1 already has its own proposal.
        Node 2 and Node 3 now also have the proposal from Node 1.

    At this point, all three nodes (Node 1, Node 2, and Node 3) meet the quorum requirement and can begin the prevote phase.

Thus, all three nodes simultaneously transition to the prevote phase after receiving and validating the proposal from Node 1.