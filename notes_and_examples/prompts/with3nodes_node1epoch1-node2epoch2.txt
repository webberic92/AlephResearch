Yes, your understanding aligns with the expected behavior of ch-RBC for 3 nodes. Here's how the sequence would progress:

    Epoch 1 (Node 1 proposes):
        Node 1 proposes a transaction to Node 2 and Node 3.
        Once the proposal is received and validated, all three nodes (1, 2, 3) begin the prevote phase for epoch 1.
        After the prevote phase reaches consensus, the commit phase finalizes the proposal for epoch 1.
        Epoch 1 completes.

    Epoch 2 (Node 2 proposes):
        Node 2 becomes the proposer and sends its transaction to Node 1 and Node 3.
        Once the proposal is received and validated, all three nodes begin the prevote phase for epoch 2.
        After consensus is reached in the prevote phase, the commit phase finalizes the proposal for epoch 2.
        Epoch 2 completes.

    Epoch 3 (Node 3 proposes):
        Node 3 becomes the proposer and sends its transaction to Node 1 and Node 2.
        Once the proposal is received and validated, all three nodes begin the prevote phase for epoch 3.
        After consensus is reached in the prevote phase, the commit phase finalizes the proposal for epoch 3.
        Epoch 3 completes.

Summary:

    In each epoch, the designated proposer (rotating among the nodes) sends a proposal.
    All nodes participate in prevote and commit phases for that epoch.
    The proposer role rotates among the nodes: Node 1 → Node 2 → Node 3 → Node 1 (cycle repeats).

This is consistent with the ch-RBC protocol, where every node takes a turn as the proposer, and all nodes participate in the consensus phases for each epoch.