Step-by-Step Execution for 3 Nodes in Epoch 1

Let's break down the expected behavior step by step, considering the scenario where Node 1 proposes to Node 2 and Node 3:
Step 1: Proposal Phase

    Node 1 (Proposer):
        Node 1 creates a proposal and sends it to both Node 2 and Node 3.
        Node 1 is aware that its own proposal satisfies the condition to begin the prevote phase (as it has 1 valid proposal).
        Question 1 (Node 1's behavior):
            Yes, Node 1 can immediately begin sending prevotes to Nodes 2 and 3 based on its own proposal.

    Node 2 and Node 3 (Receivers):
        Upon receiving the proposal from Node 1:
            Validate the proposal (e.g., check the Merkle root and proofs).
            Recognize that 1 valid proposal satisfies the threshold to begin the prevote phase.
        Both Nodes 2 and 3 proceed to send prevotes to Node 1 and each other.

Step 2: Prevote Phase

    Node 1:
        Already started the prevote phase by sending prevotes to Nodes 2 and 3.
        Node 1 also expects to receive prevotes from Nodes 2 and 3.
        Upon receiving 2 prevotes (from Nodes 2 and 3), Node 1 validates the prevotes and moves to the commit phase.

    Node 2:
        Sends its prevote to Nodes 1 and 3 upon receiving and validating the proposal.
        Waits for prevotes from Node 1 and Node 3.
        Upon receiving 2 prevotes (from Node 1 and Node 3), Node 2 validates the prevotes and moves to the commit phase.

    Node 3:
        Sends its prevote to Nodes 1 and 2 upon receiving and validating the proposal.
        Waits for prevotes from Node 1 and Node 2.
        Upon receiving 2 prevotes (from Node 1 and Node 2), Node 3 validates the prevotes and moves to the commit phase.

Step 3: Commit Phase

    Node 1, Node 2, and Node 3:
        Each node sends a commit message to the other two nodes upon receiving a quorum of 2 prevotes.
        Each node waits for 2 commit messages (from the other two nodes).
        Upon receiving 2 commits, the nodes finalize the commit phase for the current epoch.

Step 4: Epoch Transition

    Once the commit phase is finalized for the current epoch:
        All three nodes agree on the finalized state for epoch 1.
        The system transitions to epoch 2.
        Each node resets its state for the next epoch (e.g., clearing proposal and prevote data).

Summary of Key Actions
Question 1:

    Does Node 1 immediately begin prevoting, or do Nodes 2 and 3 initiate prevotes?
        Both happen:
            Node 1 immediately begins prevoting upon sending its proposal because it knows its proposal satisfies the threshold.
            Nodes 2 and 3, upon receiving the proposal, independently begin prevoting because they know 1 valid proposal satisfies the threshold.

Overall Workflow:

    Node 1 proposes to Nodes 2 and 3.
    All nodes (Node 1, Node 2, and Node 3) begin prevoting independently as soon as the proposal condition is satisfied.
    Prevote Phase:
        Each node sends prevotes to the others and waits for 2 prevotes to proceed.
    Commit Phase:
        Each node sends commit messages to the others and waits for 2 commits to finalize the epoch.
    Epoch Transition:
        Epoch increments to the next one after the commit phase finalizes.