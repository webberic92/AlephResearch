ch-RBC:
/* (Protocol for node P i with sender P s
broadcasting unit U of round r )
1 if P i = P s then
2
{s j }j ∈N ←shares of (f +1, N )-erasure coding of U
3
h ← Merkle tree root of {s j }j ∈N
4
for j ∈ N do
5
bi ← Merkle branch of s j
6
send propose(h, b j , s j ) to Pj
7 upon receiving propose(h, b j , s j ) from Ps do
8
if received_propose(Pi , r ) then
9
terminate
10
if check_size(s j ) then
11
wait until Di reaches round r − 1
12
multicast prevote(h, b j , s j )
13
received_propose(Pi , r ) = True
14 upon receiving 2f + 1 valid prevote(h, ·, ·) do
15
reconstruct U from s j
16
if U is not valid then terminate
17
wait until all U ’s parents are locally output by Pi
18
interpolate all {s j0 }j ∈N from f + 1 shares
19
h 0 ← Merkle tree root of {s j0 }j ∈N
20
if h =h 0 and commit(Ps , r , ·) has not been send then
21
multicast commit(Ps , r , h)
22 upon receiving f + 1 commit(Ps , r, h) do
23
if commit(Ps , r, ·) has not been send then
24
multicast commit(Ps , r , h)
25 upon receiving 2f + 1 commit(Ps , r , h) do
26
output U decoded from s j ’s
each node checks the size of the proposed unit (line 10) to prevent
the adversary from sending too big units and hence consuming too
much bandwidth of honest nodes24 . Second, before multicasting
prevote, we wait until ch-DAG of a node reaches at least one round
lower then round of prevoted unit (line 11). This check counters
an attack in which adversary initializes ch-RBC for meaningless
pieces of data of rounds which do not exist in the ch-DAG yet, hence
wasting bandwidth of honest nodes. Third, after reconstructing
a unit U , we check if U is valid (line 16), i.e., we check if it has
enough parents, if one of its parents was created by U ’s creator,
and if the data field of U contains all required components. Finally,
before multicasting commit message for unit U , nodes wait until
they receive all parents of U via RBC (line 17) to ensure that it will
be possible to attach U to ch-DAG25 .
The following lemma summarizes the most important proper-
ties of ch-RBC protocol. Note that Latency is a strictly stronger
condition then Termination, and similarly Fast Agreement is
stronger then Agreement. Thus, in particular, this lemma proves
that ch-RBC implements Reliable Broadcast.
Lemma F.1. (reliable broadcast) For any given round r , all ch-RBC
runs instantiated for units of that round have the following properties:
(1) Latency. ch-RBC instantiated by an honest node terminates
within three async-rounds for all honest nodes.
(2) Validity If an honest node outputs a unit U in some instance
of ch-RBC, U had to be proposed in that instance.
(3) Fast Agreement. If an honest node outputs a unit U at
async-round r a , all honest nodes output U before async-
round r a + 2 ends,
(4) Message Complexity. In total, all the ch-RBC instances
have communication complexity26 at most O(tr + N 2 log N )
per node, where tr is the total number of transactions input
in honest units of round r .
*/
check_size(s j ):
T ← number of transactions in U inferred from s j -th size
/* C B is a global parameter of the protocol */
29
if T > C B ·(i-th batch size in round r ) then
30
output False
31
else output True